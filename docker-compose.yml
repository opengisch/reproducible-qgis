version: "3"

services:
  qgis:
    build: ./qgis
    environment:
      - DISPLAY=${DISPLAY}
    volumes:
      - /tmp/.X11-unix:/tmp/.X11-unix
      - ./io/:/io/
      # TODO: host .qgis directory?
    privileged: true
    network_mode: host
